import { WeatherTempIconDirective } from './weather-temp-icon.directive';

describe('WeatherTempIconDirective', () => {
  it('should create an instance', () => {
    const directive = new WeatherTempIconDirective();
    expect(directive).toBeTruthy();
  });
});
